# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Service account key (test credentials â€” never commit)
functions/sa-key.json
functions/venv/

# E2E test harness (contains tokens — never commit)
scratch/tests/

functions/__pycache__/
functions/functions.yaml

# Claude Code per-machine settings (can capture tokens in allow-rules)
.claude/settings.local.json

# Probe output (screenshots + throwaway browser profiles from the E2E probes)
screenshots/
scratch/
